{
  "name": "action-check_pr_basebranch",
  "version": "0.2.0",
  "description": "Check if pull request is opened against the allowed base branch upstream",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && ncc build lib/main.js",
    "format": "prettier --write **/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pllim/action-check_pr_basebranch.git"
  },
  "keywords": [],
  "author": "",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/pllim/action-check_pr_basebranch/issues"
  },
  "homepage": "https://github.com/pllim/action-check_pr_basebranch",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^4.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.27.0",
    "prettier": "^2.2.1",
    "typescript": "^4.1.3"
  }
}
